plugins {
    id 'java'
    id 'gg.essential.loom' version '0.10.0.+'
    id 'net.kyori.blossom' version '1.3.0'
}

group = 'com.futureclient'
version = '1.0.0'
archivesBaseName = 'FutureClient'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

loom {
    forge {
        pack200Provider = new dev.architectury.pack200.java.Pack200Adapter()
    }
}

// These repositories are for YOUR MOD'S dependencies (like Minecraft itself)
repositories {
    mavenCentral()
    maven { url "https://repo.spongepowered.org/maven/" }
    // Add these just in case your mod dependencies need them
    maven { url "https://maven.minecraftforge.net/" }
}

dependencies {
    minecraft "com.mojang:minecraft:1.8.9"
    mappings "de.oceanlabs.mcp:mcp_stable:22-1.8.9"
    forge "net.minecraftforge:forge:1.8.9-11.15.1.2318-1.8.9"

    implementation 'com.google.code.gson:gson:2.8.9'
}

processResources {
    inputs.property "version", project.version
    inputs.property "mcversion", "1.8.9"

    filesMatching("mcmod.info") {
        expand "version": project.version, "mcversion": "1.8.9"
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = "UTF-8"
}
